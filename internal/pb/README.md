# Shared Go API bindings

This directory holds the Go message and service definitions derived from
`protos/`.

The repository does not yet run `protoc` in-tree, so these bindings are kept
as hand-maintained generated-style code for now.
